This is an example of using ats to test a code and its postprocessor. 
These are python scripts so no compiling would be needed but other than that
it is the same idea as when a compiled code is used, except that the
executable will be of the form "python mybigcode.py" instead of "yourcode".
